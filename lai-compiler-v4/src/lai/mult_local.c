multiplyNumbers(a : int, b : int) : int {
	
	return doTask()
	
	addNumbers(num1 : int, num2 : int) : int {
		return num1 + num2
	}
	
	doTask() : int {
		output : int
		for(i -> a) {
			output = addNumbers(output, b)
		}
		return output
	}
}